# identar o codigo define onde inicia e finaliza o codigo

def sacar(valor): # Inicio da função sacar
    saldo = 500
    if saldo >= valor: #Inicio da condição IF
        print("Valor sacado!")
    # fim da condição if
# fim da função sacar
sacar(100) #Chama a função e passa o parametro valor > 100